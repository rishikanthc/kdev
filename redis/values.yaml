global:
  storageClass: nfs-crimson

image:
  tag: 7.0.12

existingSecret: redis-secret
existingSecretPasswordKey: password

persistence:
  storageClass: nfs-crimsons
  size: 1Gi

persistentVolumeClaimRetentionPolicy:
  enabled: true
